package org.akonakanchi.java.dataTypes;

import com.sun.jdi.InterfaceType;

import java.lang.reflect.Array;

public class nonPrimitiveDataTypes {
    // Sequence of characters
    String s = "What is this";
    // collection of similar dataTypes, accessed by index
    int[] intArray = {1,2,3,4};
    String[] stringArray = {"abc","def","ghi"};

    //CLASS blueprint for creating Object, providing initial values for member variables
    // Interface is a collection of abstract methods, a class that implements a interface must implement all the methods in interface
    //Enum special data type that allows programmer to define a set of named values
    //Record special data type allows programmer to define a "carrier" of data



}
